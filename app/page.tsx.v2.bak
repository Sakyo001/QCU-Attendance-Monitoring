import Link from "next/link";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { ShieldCheck, UserCheck } from "lucide-react";

export default function Home() {
  return (
    <div className="min-h-screen bg-slate-50 flex items-center justify-center p-4">
      <div className="max-w-4xl w-full grid md:grid-cols-2 gap-8">
        <div className="space-y-4 text-center md:text-left flex flex-col justify-center">
          <h1 className="text-4xl font-bold tracking-tight text-slate-900">
            Attendance Monitoring System
          </h1>
          <p className="text-lg text-slate-600">
            Secure, fast, and automated facial recognition attendance for modern institutions.
          </p>
        </div>

        <div className="grid gap-4">
          <Link href="/admin/login" className="block">
            <Card className="hover:border-slate-400 transition-colors cursor-pointer" >
              <CardHeader className="flex flex-row items-center gap-4 space-y-0">
                <div className="bg-blue-100 p-3 rounded-full">
                  <ShieldCheck className="w-6 h-6 text-blue-600" />
                </div>
                <div className="flex-1">
                  <CardTitle>Admin Portal</CardTitle>
                  <CardDescription>Manage students and view reports</CardDescription>
                </div>
              </CardHeader>
              <CardContent>
                <Button className="w-full">Login as Admin</Button>
              </CardContent>
            </Card>
          </Link>

          <Link href="/student/login" className="block">
            <Card className="hover:border-slate-400 transition-colors cursor-pointer">
              <CardHeader className="flex flex-row items-center gap-4 space-y-0">
                <div className="bg-green-100 p-3 rounded-full">
                  <UserCheck className="w-6 h-6 text-green-600" />
                </div>
                <div className="flex-1">
                  <CardTitle>Student Portal</CardTitle>
                  <CardDescription>Mark attendance and view history</CardDescription>
                </div>
              </CardHeader>
              <CardContent>
                  <Button variant="outline" className="w-full">Login as Student</Button>
              </CardContent>
            </Card>
          </Link>
        </div>
      </div>
    </div>
  );
}
